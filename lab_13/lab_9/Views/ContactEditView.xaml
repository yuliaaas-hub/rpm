<UserControl x:Class="lab_9.Views.ContactEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:lab_9.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Редактирование контакта" FontSize="20" FontWeight="Bold" />
        <TextBox Text="{Binding EditContactName, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
        <TextBox Text="{Binding EditContactPhone, UpdateSourceTrigger=PropertyChanged}" Margin="0,10"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="5"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>
    
 
